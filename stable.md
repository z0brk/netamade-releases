# 哪吒美式 v4.6.3 版本更新日志

本次更新扩展智驾小蓝灯、美式 Dock 与 OP 助手能力，并补充内存策略、行程记录、氛围灯和数字座舱数据支持，持续修复车辆控制与守护场景的稳定性问题。

## 一、智驾小蓝灯

- 新增蓝灯状态灯条，可拖动调整位置和尺寸，并在行驶挡位显示；非 D 档自动隐藏。
- HTTP 连接支持 ESP32 内置控制、状态查询和网络入口，可通过二维码或手动地址访问设备。
- 解析设备 HTTP 状态响应并复用语音提醒；上车首次 OFF 状态不再误播关闭提醒。
- 语音指令优先发送控制命令，未启用对应协议时按默认配置回退发送；补齐蓝牙 GATT 通知和语音分发链路。

## 二、美式 Dock

- 应用启动器新增车辆控制首行，可固定高频控制项并长按排序；新增一键通风控制及对应 Dock 组件。
- 主屏与副屏支持独立开启 Dock、独立编辑布局和独立自动隐藏策略，可按应用设置显示、隐藏或跟随默认行为。
- 按屏处理 Dock 手势与屏保隐藏，修复副屏车门状态和自动隐藏相关显示问题。
- 修复图标大小设置无效并优化网格布局。

## 三、OP 助手
> AI 超车为辅助驾驶实验能力，仍需驾驶员确认并持续承担驾驶责任；需满足 OP、摄像头、盲区信号和相关服务可用条件。

- 新增 AI 超车功能：分阶段采集和评估路况，展示当前方案与执行状态，并通过语音完成确认、停止和结果播报。
- AI 超车复用自动变道与盲区安全校验，执行结束后恢复原巡航速度；等待 OP 接管时增加宽限处理，减少状态抖动导致的误结束。
- 自动变道联动盲区辅助转向信号，提升变道过程中的安全拦截和状态一致性。

## 四、行程能耗

- 手动记录模式下挂入 P 挡时改为询问是否结束当前行程；确认后仅保存行程，手动记录仍保持开启。
- 新增“驻车结束提醒”开关，关闭后驻车不再弹出结束询问。

## 五、氛围灯

- 修复音乐律动不变色、亮度偏暗及与原车音乐律动冲突的问题，弱信号下保持基础亮度并持续刷新颜色。

## 六、车辆控制

- 声音增益设置移入“声音”页面，新增车外大喇叭增益滑块，支持调节车外播放音效的输出音量。
- 修复无感进出优化手动关闭后无法自动恢复的问题；座椅控制改为动态读取账号坐姿编号。
- 智能场景在锁车和远程上电期间阻止误触舱内设备，减少空调、座椅等动作误触发。

## 七、乾坤守护

- 修复守护启动期间气泡和灵动岛提示导致的崩溃。
- 关键启动任务失败时改为降级放行；仅锁车不断电失败会中止启动，提升守护启动成功率。

## 八、数字座舱
> 数字座舱 CAN 数据源当前仅适配哪吒 L，其他车型不显示这些数据。
- 哪吒 L 新增从 CAN 数据源读取动力电池温度、增程器水温、发动机转速、高压、压力和电机扭矩等实时数据，可用于数字座舱展示。

## 九、问题修复

- 高德导航支持“导航 + 目的地”语音说法，并恢复车道指示图标对比度。
- 优化远程上电时的摄像头录制性能。

---

# 哪吒美式 v4.6.2 版本更新日志

本次更新重点扩展美式 Dock 的模板、组件与多屏能力，新增 OP 助手语音控速与自动跟车、限制后台运行、Traccar 车辆数据上报、类 macOS 启动台应用启动器等功能，并优化车辆控制与导航辅助体验。

## 一、美式 Dock（开发中功能）
> - 已知部分页面无法自动隐藏，无需反馈
> - 已知会和小憩模式、全屏切换等功能冲突，导致显示异常，也无需反馈
> - 已知哪吒 GT 无法隐藏原车应用栏，S 在全屏切换后存在应用栏空白的 Bug，L 适配程度较好
> - 已知媒体组件不支持部分音乐软件（不支持的软件后续也不会适配），已知专辑图经常获取不到或者歌词存在延迟，无需反馈
> - 若想临时隐藏 Dock，可直接在 Dock 上进行长距离右滑手势，会在当前应用中隐藏，切换应用后恢复显示

- 支持模板管理，内置媒体、任务、车辆状态等默认模板，可快速套用、自定义与导入导出
- 支持主屏与副屏分别配置 Dock 布局，模板可按全部/仅主屏/仅副屏范围导入导出
- 新增单行与多行日期组件，支持按用户时区显示当前日期
- 新增横条、迷你、分栏三种媒体组件，可自动识别主副屏媒体
- 新增「最近任务」组件，支持一键清理任务
- 新增可自定义图标组件，支持添加本地图标；支持组件自定义标题
- 能量续航组件合并并扩充指标，支持动态颜色
- 复合组件支持自动轮播子组件，默认双击切换，自动切换倒计时设置为 0 则手动切换
- 横向容器支持设置对齐方式，纵向容器支持自动滚动后对齐
- 各个组件支持浅色/深色模式的内容展示，默认都开启
- 支持超长右滑手势临时隐藏 Dock
- 新增应用启动台的应用程序全局浮窗（推荐使用这个，若使用原车「应用列表」无法正常关闭）


## 二、OP 助手

- 新增语音控速，可通过语音指令调整目标车速，支持中文数字与全角数字
- 新增自动控制跟车距离，按车速三档动态写入跟车距离配置
- 新增控速事件语音提醒，支持匝道、电子眼、收费站、隧道、红灯等场景的分项开关
- 新增语音指令确认播报与头枕出声开关，语音设置统一迁移到语音页签
- 变道提醒支持分项控制
- 优化提前靠边场景，目标侧无车道时仅保留语音提醒并更新悬浮窗展示
- 控速提速直接下发目标速度上限，避免提速时车速过慢
- 修复锁车解锁后配置读取静默失效

## 三、应用管理

- 一键授权默认不开启后台运行/自启动
- 支持一键禁止应用后台运行/自启动
- 新增应用启动台的应用程序全局浮窗，支持搜索、分类浏览、长按菜单与外观自定义
- 应用抽屉支持切换为类启动台样式，触发后以全局浮窗展示

## 四、限制后台运行
> 这就是类似 **冰箱**、**黑域** 的应用后台运行限制功能，支持自定义管理后台应用的行为，避免后台应用占用过多资源或影响前台体验。
- 新增应用后台运行限制功能，可自定义管理后台应用
- 支持设置离开前台后的宽限时间
- 支持按应用配置后台行为，保护媒体与前台服务

## 五、乾坤视窗

- 新增应用任务跨屏模式，支持无重启搬移到副屏
- 新增自动隐藏 OEM 底部应用栏开关

## 六、乾坤守护
- 修复获取车机锁车信号失败导致的预警录制未启动
- 支持雷达检测单独开关，可配置全部或单个距离雷达
- 锁车信号读取失败时按已锁车兜底
- 优先保留预警录像并优化清理，不再按录制时间排序导致优先覆盖预警视频

## 七、Traccar

- 新增车辆数据上报模块
- 支持将车辆位置和所选状态发送到用户配置的 Traccar 服务器
- 支持自定义服务器地址、设备 ID 与上报间隔
- 提供上报历史记录查看

## 八、高德导航辅助

- 修复 ETA 到达时间文案重复与加粗异常
- 修复车道图标与推荐状态
- 修复红绿灯在组件边界内居中对齐

## 九、其他

- 【盲区辅助】修复应用重启后无法显示的问题
- 移除"倒车时允许手动调节后视镜"智能场景
- 重启检测在检测到系统应用自身异常导致异常时弹出「清除数据」提示
- 智驾小蓝灯增加蓝牙数据包回读语音提醒
- 配置传输分享码兼容 4 至 6 位导入

---

# 哪吒美式 v4.6.1 版本更新日志

本次更新重点完善美式 Dock 的组件体系与编辑体验，扩展 AI 助手、OP 助手、高德导航辅助和任务系统的控制能力，并统一应用更新与配置传输入口。

## 一、美式 Dock（开发中功能）

- 支持在独立编辑页中自定义 Dock 结构，自由添加、排序、编辑和删除组件
- 支持数据类、应用类、任务类、空调类、控制类、复合类等多种组件类型
- 支持胎压、档位、电量、油量、续航、电驱通勤、电池储能、增程总览等车辆状态数据展示
- 支持空调温度、风速、循环模式、A/C、AUTO、双区同步、前后除霜、座椅加热/通风等控制组件
- 支持横向容器、纵向容器与复合组件，可多层嵌套组合
- 支持为组件配置数据源切换与自动轮播
- 支持从已安装应用列表添加快捷入口
- 支持将执行任务和可切换任务绑定到 Dock 组件
- 支持为组件配置单击、长按、上下左右滑动等手势行为
- 支持调整图标大小、组件间距、字体缩放、组件缩放等显示参数
- 编辑器采用草稿模式，支持撤销修改和恢复默认布局

> - 美式 Dock 仍在开发中，目前仅哪吒 L 适配程度较好，其他车型目前无人配合测试效果可能存在显示或交互问题。
> - 哪吒 L 可用的分享码：
>   - `6279YX`: 来自 S 哥的空调组件左滑降低风速、右滑增加风速 档位组件单击循环切换驾驶模式（运动舒适节能）
>   - `5882IK`: 来自 Pan 的

## 二、AI 助手

- 支持通过 AI 助手语音控制主屏、副屏的开启与关闭
- 支持通过 AI 助手语音开启或关闭屏幕清洁模式（需 P 档）

## 三、OP 助手

- 支持语音控制 OpenPilot：向左变道、向右变道、开启领航、关闭领航，可自定义唤醒词
- 支持导航启动时同步开启 OpenPilot 领航
- 连续变道支持超时重置与失败重试衔接
- 扩展转向车道提前处理，禁止导航方向相反的自动变道
- 修正单侧路沿车道定位与导航选道方向偏差
- 优化变道执行逻辑，未执行变道不再提前收尾
- 连接后自动恢复配置读取，完善配置传输与同步日志

## 四、高德导航辅助

- 悬浮窗支持后台显示开关，可在高德地图后台时继续展示导航信息
- 统一分叉与环岛图标语义，统一扩展转向图标语义
- 修复车道可走性高亮、占位项错位、方向表缺失等问题
- 修复红绿灯闪烁、黄灯过渡态显示与方向展示
- 扩展电子眼类型表，下一服务区字段接通，限速显示支持电子眼限速回退
- 修复搜索结果不可用，避免搜索结果缓存串用
- 修复直接算路入口坐标判断
- 校对广播协议与 HUD 语义

## 五、任务系统

- 「任务语音控制」升级为「任务行为控制」，支持全局与任务级语音提醒开关
- 支持全局与任务级灵动岛提示策略设置
- 支持灵动岛与气泡两种提示样式切换
- 任务选择对话框支持语音搜索
- 座椅与空调任务支持循环切换

## 六、通知中心

- 支持灵动岛与气泡两种临时通知样式切换

## 七、应用更新

- 统一全局更新下载入口
- 下载进度接入悬浮窗管理，支持折叠悬浮球与展开卡片两种形态
- 统一安装入口，避免重复安装

## 八、主页重定向

- 主页重定向支持恢复上一个应用
- 可配置主页重定向时优先跳转到上一个应用

## 九、配置传输

- 统一配置分享入口，支持创建分享码、导入分享码、导入文件、导出文件
- OP 助手配置分享支持领航、变道、OP 本地配置三段内容
- 美式 Dock 布局支持导出、导入与分享

## 十、乾坤守护

- 展示未生效功能的具体原因
- 恢复升级后缺失的预警录像

## 十一、融合定位

- 增加 GNSS 会话保活与断流确诊日志
- 修复注入后定位冻结与引擎盲启动
- 修复巡航车标冻结，增加引擎停摆重建

> 融合定位为实验性功能，仅供哪吒 L 使用，其他车型不兼容。

## 十二、其他功能修复

- 增加媒体切换过渡效果
- 修复图标缓存共享 Drawable 实例导致的并发绘制崩溃
- 修正媒体广播监听包名解析

---

# 哪吒美式 v4.6.0 版本更新日志

> 本次更新重做美式 Dock 的运行时布局与编辑器，新增工作流灵活时间触发；高德导航、OP 助手、融合定位、AI 助手与哪吒语音助手同步完成一批功能扩展和稳定性修复。

## 一、美式 Dock（开发中功能）

- **仅支持哪吒 L**：美式 Dock 仍在开发中，目前存在较多问题，请谨慎开启并保留原车底栏的使用预期
- **布局编辑器重做**：支持按区域调整 Dock 布局，添加、排序和编辑空调区、数据区、功能区；可从已安装应用、执行型任务和可切换任务中添加快捷位
- **交互与保存流程**：编辑布局采用草稿模式，确认后才应用；支持撤销编辑和恢复默认布局，避免未完成调整直接影响运行中的 Dock
- **显示优化**：Dock 统一为纯黑背景并补充交互反馈；打开原车空调页面时会自动让出 Dock 显示区域

## 二、工作流

- **新增灵活时间触发**：新增「间隔触发」与「计划定时触发」条件，可在车辆未下电期间按固定间隔，或按指定星期和时间窗口执行工作流

## 三、OP 助手

- **自动变道优化**：新增激进连续变道模式；优化环岛、可走车道、0 米转向等导航场景的决策，减少错误变道或未触发变道
- **减速策略优化**：完善交通灯减速方向判断；经过高速出口但继续沿主路行驶时，会及时退出匝道限速，避免被错误压低车速
- **配置管理修复**：改善连接重试、导入导出提示，并修复云端导入配置无法同步的问题

## 四、高德导航辅助

- **悬浮窗组件化**：OP 助手信息与融合定位信息统一作为高德导航悬浮窗组件展示；悬浮窗支持内容对齐，并会随组件数量自适应宽度
- **HUD 信息扩展**：高德导航 HUD 可展示 OP 助手与融合定位组件；浅色背景下的悬浮文字可读性更好
- **导航语义修复**：区分可走高亮车道与路线推荐车道，统一导航辅助文案；恢复后台巡航播报的兼容链路
- **哪吒音色**：高德导航辅助页面支持哪吒音色配置与试听，恢复可用音色列表并支持在线音色；移除无效音色及原车同步配置，减少错误选项
- **融合定位（实验性功能）**：仅供哪吒 L 使用，其他车型不兼容，开启后会导致没有定位。弱信号场景可持续提供定位，输出异常时会自动回退至增强定位
  > **实测情况**：已行驶约 700 km、经过 20 余个隧道，最长约 5 km 的隧道内未丢失定位；期间有 3 次触发路线重新规划，但是仍会在正确位置重新开启导航，不会像以前一样丢失位置方向而错乱

## 五、AI 助手（奔奔）

- **模型配置**：新增 Agnes AI 免费模型预设；修复工具调用结果的展示顺序
- **副驾显示**：支持将副驾唤醒后的对话内容显示在副驾屏，可在设置中独立开关
- **悬浮对话**：对话悬浮窗支持自由移动，修复透明度变化时的闪烁问题
- **车控兼容性**：兼容“关闭天窗”等工具参数，提升相关语音车控的执行成功率

## 六、其他优化

- **授权自动续期**：原车麦克风、摄像头与定位授权剩余时间不足时自动续期至 364 天；偏好设置中可手动一键续期到 9999 天
- 按下主页键会稳定回到已配置的主页目标，修复跳回当前应用形成回环的问题
- 修复天窗控制属性取值，恢复天窗任务的正常执行
- 修复哪吒 S 的屏幕清洁功能异常
- 非行车或下车场景不再触发驻车开门、盲区变道音效，减少不必要的提示
- 删除录像时会同步清理关联通道文件，避免遗留无效录像占用存储空间
- 统一车内外温度有效性处理，减少异常温度值对相关功能状态的影响
- 当前应用小窗恢复默认处理方式，提升常规使用场景的显示稳定性
