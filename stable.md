# 哪吒美式 v4.6.7 版本更新日志

本次更新重点新增 OP 助手远光爆闪、智能场景「窄路安心」「通勤自动导航」「低续航提醒」三大场景与盲区辅助多种自动触发方式，乾坤守护支持多类型融合触发策略，并新增「隐身休息」任务与远程车控消耗统计，同时修复锁车提醒、副驾坐姿、后雾灯爆闪等一系列问题。

## 一、OP 助手

> **注意事项**
> 「爆闪」依赖 OP 设备实现，未连接 OP 设备时无法使用；语音指令与任务触发同样要求 OP 设备在线。

- 新增「爆闪」分页，可自定义「闪远光次数」（1-30 次）与闪光间隔（50-3000 毫秒），点击「开始爆闪」按当前参数爆闪远光灯。
- 任务「Openpilot 控制」新增「远光爆闪」命令，语音说“闪一下远光”等也可按当前参数触发，唤醒词可在「语音」页修改。
- 新增配置一键导入到本地，方便快速迁移或恢复 OP 助手配置。
- 修复「控速提醒」在巡航速度已满足要求时误播报的问题，巡航速度已不高于要求限速时不再播报减速与恢复限速提醒。

## 二、智能场景

- 新增「窄路安心」场景，D 档低速行驶时车身雷达检测到过近障碍物，自动开启 360 全景影像或探路模式窄路视角（探路模式限支持盲区辅助的车型），脱离后自动关闭，触发距离与车速可调。
- 新增「通勤自动导航」场景，所选工作日通勤时段主驾系上安全带后，自动（或倒计时询问后）发起去公司/回家导航，支持原车导航与高德车机版；时间窗可自定义并支持跨零点班次，同一时段每个班次只发起一次。
- 「通勤自动导航」新增「联动动作 → 同步开启空调」和「联动动作 → 同步开启主驾座椅通风」开关（默认关闭），执行场景发起导航时同步打开对应功能，取消确认框时不会开启。
- 新增「低续航提醒」场景，行驶中电量穿越两级阈值（默认 20%/10%）时语音提醒，锁车时电量低于阈值（默认 10%）远程推送通知，两者可独立开关。
- 「低速安心」新增「触发方式」设置，可在 360 全景影像与探路模式四网格视角之间选择（探路模式限支持盲区辅助的车型），只自动关闭由场景开启的探路模式。

## 三、盲区辅助

- 新增「近距离跟车」触发，D 档低速行驶时前/后方障碍物距离过近自动弹出对应方向画面，可分别启用前方/后方并调整触发距离与最高触发车速。
- 新增「自动驻车」触发，D 档自动驻车停稳后自动弹出前视/后视画面（默认前后双视，可单独关闭后视），起步或离开 D 档后自动关闭。
- 新增「窄路触发」，D 档低速通过窄路时任一侧向雷达距离过近自动弹出对应侧画面，默认开启左右视角，可调整触发距离（20-60cm）与最高触发车速。

## 四、乾坤守护

- 新增「触发策略」分页，支持按雷达、AI 视觉、物理入侵多类型融合判定后才触发声光提醒和远程推送，并可按类型选择不检测、仅视频标记、声光提醒或加远程推送；未满足策略的预警仍会记录到视频标记，事故撞击和拖车预警始终提醒。
- 修复远程临时调整「雷达灵敏度」（侧向/前后雷达触发距离）后预警状态未及时刷新的问题。

## 五、任务系统

- 新增「隐身休息」任务，支持弹窗选择休息方案，或按指定方案直接进入隐身休息，且不受智能场景开关限制。
- 修复「车辆音量控制」「屏幕亮度控制」「无线充电」任务确认选择后参数配置被直接取消的问题，音量类型、屏幕选择后可正常进入下一步设置。

## 六、远程车控

- 新增「露营模式」和「临停舒适」消耗统计卡片，车辆页展示运行时长、消耗电量与消耗续航，应用重启后继续累计。
- 支持远程开关露营模式。

## 七、应用启动器

- 新增「车辆控制」快捷操作，支持空调、座椅和冰箱控制。

## 八、问题修复与功能优化

- 【声音】新增「声音 → 音频增益 → 车外大喇叭增益」，支持 0-24dB 调节，放大车内外同时播放或仅车外播放时的车外响度。
- 【锁车冰箱维持】新增「锁车自动恢复」，锁车时冰箱维持被车辆清除后自动重新开启，反复解锁闭锁也能持续制冷。
- 【核心引擎】新增「远程上电时执行上电恢复」开关，远程上电时自动恢复全部功能。
- 【Traccar】新增「过滤零精度定位」开关（默认开启），定位精度为 0 时跳过本次上报，避免发送不可靠位置。
- 【锁车提醒】修复「未锁车提醒」在车内躺下休息或频繁移动时误报的问题，主驾有无人改为结合车门、安全带与座椅占用综合判断；同时修复应用覆盖升级或进程重启后不触发的问题，应用重启、亮屏或车辆解锁后自动恢复车内无人检测。
- 【座椅控制】修复「副驾坐姿」加载和保存实际执行为主驾坐姿的问题；升级前已创建的副驾坐姿快捷方式需重新创建。
- 【后雾灯爆闪】修复爆闪结束后后雾灯残留开启的问题，灯光状态恢复不再误报失败。
- 【智驾小蓝灯】修复应用重启后悬浮灯条未恢复的问题。
- 【乾坤视界】修复「Dock 乾坤视界组件」上电后自动弹出四路画面的问题。
- 【车辆状态】修复不同车型的座椅位置、乘员状态和刹车压力数据读取异常，以及只写属性设置后误报失败的问题。
