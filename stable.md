# 哪吒美式 v4.6.5 版本更新日志

本次更新重点增强盲区辅助画质与触发体验，新增车辆数据共享、美式 Dock 一键通风多档手势、应用启动器自定义分类，并补齐工作流车控任务参数、支持语音启动乾坤守护，同时修复低速安心、OP 助手与无线热点等稳定性问题。

## 一、盲区辅助

> **注意事项**
> 画面去畸变默认关闭，开启后按车型自动匹配校正参数；GT 智驾版、22S 不支持该功能。

- 修复 OP 助手「自动变道」与「按键变道」场景下转向灯小窗不触发的问题，变道时盲区辅助窗口可正常弹出。
- 新增「窗口圆角」配置，可在「全部功能 → 辅助 → 影像守护 → 盲区辅助 → 窗口设置 → 窗口圆角」中调整悬浮小窗圆角，默认 16 dp，设为 0 时关闭圆角。
- 新增「画面去畸变」开关，开启后对盲区辅助预览画面进行去畸变处理，提升侧向画面平直度，录像仍保持原始画面。
- 工作流任务库新增「盲区辅助」任务，支持「左转灯」「右转灯」「左转角」「右转角」「左盲区来车」「右盲区来车」「主驾开门」「副驾开门」「全部关闭」等动作。

## 二、工作流

- 补齐车控任务参数配置：「车辆音量控制」支持选择媒体/导航/语音/通话/系统音量，并可设置 0–100% 目标音量或静音；「屏幕亮度控制」支持选择主屏/仪表盘/副驾屏并设置 0–100% 亮度；「无线充电」支持选择开启或关闭。

## 三、车辆数据共享

> **注意事项**
> 该功能默认关闭，开启后仅对已授权应用提供数据；锁车后会继续保持共享。

- 新增「车辆数据共享」页面，默认关闭，开启后允许其他应用读取车辆实时状态。
- 支持共享车速、电机转速、增程转速（增程车型）、锁车状态、电源状态、外放大喇叭等数据。
- 新增「允许大喇叭控制」子开关，外部应用默认仅可读取数据，开启后才可控制外放大喇叭。

## 四、美式 Dock

- 「一键通风」组件支持多档手势：单击切换通风位或全关，双击切换全开或全关，长按切换半开或全关；执行过程中会识别车窗实际位置与忙状态，避免重复操作。

## 五、乾坤守护

- 支持内置语音启动，可通过「开启乾坤守护」「打开停车监控」「启动哨兵」等语义组合直接启动守护；若工作流中已配置同名自定义任务，优先执行自定义任务。

## 六、应用启动器

- 应用抽屉新增「自定义应用」分类，可将常用应用单独归为一组浏览。
- 支持长按应用图标快速「添加到自定义应用」或「移出自定义应用」，主屏与副屏的自定义列表分别维护并实时同步。

## 七、存储管理

- 优化录制存储自动分配策略：预警录制优先保留约 100 段默认时长的预警视频，持续录制优先保证乾坤视界 8 小时、乾坤守护 24 小时，仍有空间时按「视界 3 天、守护 3 天」轮换追加，容量不足时均分兜底。
- 录制存储新手指引新增「预计可回放时长」估算，按当前画质与录制模式实时显示乾坤视界 / 乾坤守护可保存的连续录像时长。
- 到达容量上限时改为「优先清理最旧的普通循环视频」，明确预警视频、手动标记重要视频和锁定的守护会话不会自动清理。

## 八、问题修复与功能优化

- 【低速安心】修复手动关闭 360 全景影像后仍被重复自动打开的问题，现在通过方向盘按键、乾坤圈或原车 AVM 界面左上角 X 主动关闭后，在抑制时长内不会再次自动开启。
- 【OP 助手】移除 AI 超车功能及相关设置入口；修正自动变道语音播报文案，避免「前方右前方」类错误口播，已提前靠边场景统一提示保持当前车道。
- 【无线热点】修复任务系统自动开启热点时，系统无线模块启动失败或预热超时后遗留 WiFi 的问题，提升自动开启成功率。
