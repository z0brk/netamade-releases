# 哪吒美式 v4.1.5 版本更新日志
- 【远程车控】连接断开时，自动关闭投送
- 【盲区辅助】修复悬浮窗丢失摄像头画面

---

# 哪吒美式 v4.1.4 版本更新日志

本次更新重点推出远程车控功能，支持局域网内车辆状态查询与远程控制，同时大幅增强语音识别能力，并修复行程能耗多项数据准确性问题。

## 一、远程车控

远程车控是一个全新的功能模块，入口位于侧边栏「远程车控」页签。该功能让您在同一局域网或公网的设备上查看车辆状态，并可执行白名单内安全车控指令。

- 新增**局域网网关**：开启后，同网络内设备可通过 WebSocket 实时读取车辆状态数据，涵盖能量、驾驶、车身、空调、座椅、灯光、音效、屏幕、胎压、位置、守护统计等 12 大类
- 新增**公网车控同步**：开启后，车辆通过 MQTT 主动同步状态，支持远程接收授权请求；连接强制使用 IPv4，WiFi 变化时自动重连
- 新增**实时画面 P2P 推流**：支持动态分辨率切换和视角选择，采用抢占式会话管理避免连接残留
- 新增**远程控制指令**：支持车门锁、车窗、天窗、遮阳帘、后备箱、后视镜、灯光、空调、座椅、音效、屏幕等白名单指令；P 挡下才允许锁/解锁操作，10 秒超时保护
- 新增**车辆信息规格展示**：续航达成率、电池 kWh、健康度、CPU 温度，区分增程与纯电车型
- 新增**局域网 WebSocket 实时推送**与**增量同步**车辆电源状态变更到规格信息
- 页面提供二维码扫码获取车机局域网 IP、连接信息、最近动态日志、功能介绍等辅助入口

> **注意事项**：
> - 远程车控使用教程请参见**股东群群文件**。
> - 公网车控资格不定时在**股东群**发放，无资格不可使用公网车控同步，局域网网关不受此限制。
> - 使用前需完成 8 步同意书确认。

## 二、语音识别

语音识别功能拆分为导航、应用、任务三个独立模块，支持更细粒度的开关控制和自定义配置。

- 新增**导航语音控制**：支持 60+ 条指令，覆盖高德车机版地图操作、路线策略、地点搜索、播报设置、语音角色切换等 7 大类，默认关闭
- 新增**应用语音控制**：支持语音启动、停止、重启已安装应用，可指定主驾/副驾屏幕；新增应用范围模式（全部/白名单/黑名单），默认关闭
- 新增**任务语音触发**：可为系统任务绑定自定义触发词，预置「修复定位」和「雾灯爆闪」两条规则，默认开启
- 新增**同音词容错**：对常见语音识别错误（如"美食"→"美式"、"告速"→"高速"）自动修正
- 新增**运行时语音开关**：可通过语音命令临时开启或关闭某项识别能力，无需进入设置页
- 新增**去重窗口**：500ms 内相同指令自动过滤，避免重复执行

## 三、行程能耗

修复多项数据准确性问题，让行程记录更可信。

- 修复纯电行程误显示油耗的问题：现在只有实际掉油的行程才显示油耗，纯电行驶不再出现残留油耗数据
- 修复统计页面掉电/掉油百分比跨行程累加超 100% 的问题：改为按区间首末值计算净消耗
- 修复锁车后行程未正确结束、解锁时恢复旧行程的问题：每次锁车后行程确定结束，下次启动从新行程开始

## 四、任务与工作流

- 新增「重启应用」任务：先停止目标应用，等待进程退出后自动重新打开，支持语音触发
- 修复主页劫持修改开关或参数后需重启应用才能生效的问题

## 五、守护统计

- 新增续航消耗跟踪：记录守护期间初始与当前续航里程，计算已消耗续航
- 新增每小时续航消耗统计：用于衡量守护运行对续航的影响速率
- 通知推送渠道（Bark、钉钉、邮件、飞书、PushDeer、ServerChan、企业微信）均已补充续航消耗数据

## 六、充电通知

- 新增充电剩余时间显示：充电通知中补充交/直流充电剩余分钟数

## 七、车辆电源

- 默认空气循环模式改为「保持不变」：下电后不再自动修改内外循环状态，避免干扰用户已有设置

## 八、控制中心

- 日期格式精简：移除年份显示，仅保留「月日」

注意事项：

- **HUD 导航增强功能已移除**，相关页面入口和设置项不再可用。

---

# 哪吒美式 v4.1.3 版本更新日志
- 【行程能耗】新增储电量下降指标并补充控制中心动态颜色
  - 新增储电量下降字段
  - 为控制中心行程能耗数据源补充分级动态颜色
- 【远程车控】公网车控改为使用 MQTT 协议，过滤车机的异常数据导致里程等数据异常
  - 更新后开关需要重新开启，并阅读开启提示

---

# 哪吒美式 v4.1.2 版本更新日志

本次更新重点优化了乾坤守护的启动体验与自动退出逻辑，修复了远程车控的数据同步与指令兼容性问题，并改善了全屏模式在多个页面下的稳定性。

## 一、乾坤守护

- 启动页新增「下车时自动开启」快捷开关，开启后将同步激活智能场景——在切换到 P 档并解开安全带时，自动提醒或直接启动乾坤守护
- 启动条件的检查列表改为紧凑的芯片布局，信息展示更紧凑
- 守护模式因非用户主动操作而结束时（如异常退出），车辆将自动下电，避免守护中断后车辆持续耗电

## 二、远程车控

- 公网车控探活心跳间隔从 15 秒降至 3 秒，远程指令响应速度显著提升
- 统一了车端、云端、移动端三方的车辆状态数据格式，修复了部分状态下字段不一致的问题
- 新增车门开闭状态、后备箱高度、后视镜加热、车内/车外温度等状态上报
- 新增远程关闭后备箱指令，支持通过远程直接关闭尾门
- 修复了遮阳帘、后视镜加热、空调温度同步等部分远程指令的属性映射错误
- 远程指令不可用时，现在会明确返回中文原因说明（如"车辆链路未就绪""电量不足"等），便于用户理解

## 三、行程能耗

- 修复了锁车后保存的历史行程在解锁车辆后有时会自动继续记录的问题
- 优化了行程数据持久化的并发安全性，避免极端情况下数据写入冲突

## 四、全屏模式

- 修复了乾坤守护视频库页面在开启全屏播放后，退出子页面再返回时全屏状态丢失的问题
- 修复了「标准」模式启动器页面全屏切换不生效的问题
- 修复了屏幕保护页面全屏状态恢复不可靠的问题
- 优化了全屏切换的底层实现，确保分屏状态与全屏状态同步更新

## 五、智驾提醒

- 修复了开启超大音量提醒后，若系统同时触发了静音操作，音量设置会被覆盖导致提醒音量不生效的问题

## 六、刹车压力

- 优化了刹车压力值的信号检测逻辑，移除了此前因不同系统版本切换刹车压力属性而引入的复杂自适应机制，简化为更稳定的单一检测路径

## 七、界面与交互

- 全局提示对话框（确认/取消按钮）的字体大小增大至 24sp，提升车机屏幕可读性

---

# 哪吒美式 v4.1.1 版本更新日志
- 移除“【行程能耗】百公里电耗计算未扣除动能回收电量”的改动，该算法计算的电耗数据不准确

--

# 哪吒美式 v4.1.0 版本更新日志

本次更新重点新增远程车辆网关，支持局域网和公网远程车控；新增方向盘控制记录、工作流批量操作与滑动切换、刹车压力属性自适应，并优化乾坤守护、行程能耗、通知推送、智能场景等多项功能。

## 一、远程车辆网关
- 新增「远程车辆网关」功能，同一局域网内的设备可通过 WebSocket 连接车机，实现实时车辆状态同步和远程控制。
- 支持远程锁车/解锁、空调温度与风量、前挡除霜、鸣笛/闪灯寻车、车窗通风、天窗、遮阳帘、后备箱、座椅加热/通风/按摩、氛围灯、冰箱、外放电、乾坤守护启停等远程控制能力。
- 支持读取车辆实时状态，包含车门、车窗、天窗、灯光、空调、座椅、电量等主要属性。
- 支持公网心跳同步，定时上报车辆状态并接收授权的远程控制请求（不对外开放，仅自用）。

## 二、方向盘控制
- 新增「控制记录」功能，实时展示当前绑定操作的功能执行情况，可查看每次按键操作对应的任务执行结果。
- 切歌「上一曲」「下一曲」未执行时，会记录并展示不执行的原因（如 ACC/LCC 激活中、媒体会话冲突等），方便排查控制异常。
- ACC/LCC 激活时自动禁用方向盘绑定操作的功能改为默认开启。

## 三、工作流
- 工作流列表支持左右滑动切换标签页，与顶部标签选择器双向同步。
- 新增「工作流控制」任务，支持在工作流中批量切换、启用或禁用指定工作流，可按标签或指定工作流批量操作。

## 四、任务系统
- 新增「最佳听音位置」控制任务，支持主驾、副驾、全车、后排和循环切换。
- 新增「音响模式」控制任务，支持切换车机音响模式。
- 修复「魔改状态栏」任务状态获取不正确的问题。

## 五、乾坤守护
- 调整启动时的电量检查逻辑，根据守护模式区分限制：
  - 临停舒适手动启动：电量 ≥ 30% 通过，10-30% 显示警告（可能不成功），< 10% 拒绝启动。
  - 露营模式：保持 ≥ 30% 硬性要求。
  - 智能场景自动触发：所有模式均要求 ≥ 30%，否则不触发。
- 移除退出守护后的四车窗恢复操作，避免恢复后车窗出现微开问题。

## 六、行程能耗
- 修复百公里电耗计算未扣除动能回收电量的问题，电耗数据更准确。

## 七、刹车压力
- 支持不同系统版本刹车压力属性自适应切换，自动识别并适配当前系统支持的刹车压力属性。
- 修复启动时属性状态未知导致无法切换的问题。

## 八、通知推送
- 企业微信推送默认改为纯文本模式，兼容性更好。
- Markdown 格式推送改为可配置项，需要富文本排版时可手动开启。

## 九、智能场景
- 「未锁车提醒」的提醒消息附带最后定位信息，方便确认车辆位置。
- 获取系统定位信息失败时，使用最新一次历史定位信息作为回退，减少定位信息为空的情况。

## 十、应用下载
- 支持已安装应用的覆盖安装，下载完成后直接显示覆盖安装按钮。

## 十一、智驾提醒
- 恢复智驾提醒的超大音量，提醒效果更明显。

## 十二、盲区辅助
- 修复悬浮窗弹出但无摄像头画面的问题。

## 十三、存储管理
- 修复存储风险统计被帧级重复调用导致数值虚高的问题。

## 十四、全屏管理
- 修复视频库和视频播放页全屏切换不生效的问题。