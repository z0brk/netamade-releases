# 🚀 测试版更新

## 美式 Dock
- 新增单行与多行日期组件
- 新增超长右滑手势临时隐藏 Dock
- 修复复合组件切换与图标尺寸设置问题
- 统一数据组件，支持自定义 1..5 行、1..10 列
- 去除“单数据、单应用、单任务”名称中的“单”字
- 已配置数据源改为实际内容预览，并使用带图标的添加按钮
- 新增“当前日期”数据源，按用户时区显示 yyyy年MM月dd日
- 应用组件支持选择、展示及启动副屏应用
- 原车应用列表改为再次触发时关闭，并提前同步预期显隐状态
- 座椅控制成功后打开原车座椅页面，再次触发时关闭
- 复合组件支持选择八类切换事件，默认改为双击切换
- 缺失或非法切换事件的旧复合组件自动迁移为双击
- 复合组件切换事件会屏蔽内部组件的同类事件，避免事件冲突
- 默认单击和长按行为支持禁用，禁用后允许绑定自定义任务
- 横向、纵向容器会遵循父复合组件的事件屏蔽规则
- 点击取消阈值由 20px 调整为 28px，滑动触发阈值由 36px 调整为 52px
- 修正双击第二次触摸的位移计算基准
- 编辑预览区彻底禁用控制交互，点击组件只执行选中或编辑
- 【任务系统】的】快捷功能控制支持临时开关美式 Dock
- 修复版本升级后布局被整体重置
- 新增横条、迷你、分栏三种媒体组件
- 复合组件支持自动轮播子组件
- 支持组件自定义标题
- 完善座椅组件与原车面板交互
- 按运行时导航栏状态自动隐藏
- 修复温度调节与座椅状态显示
- 统一组件默认事件管理
- 非 L 隐藏原车应用栏并改为 160px 固定双行
- 支持多模板
- 媒体组件支持自动识别主副屏媒
- 支持副屏 Dock
- 能量续航组件合并
- 新增类 macOS 启动台的应用程序全局浮窗
- 能量续航组件扩充指标并支持动态颜色
- 增加「最近任务」组件，支持一键清理
- 新增可自定义图标组件


## OP 助手

- 新增自动控制跟车距离
- 新增语音控速
- 修复锁车解锁后配置读取静默失效
- 控速提速直接下发目标速度上限，避免提速时乌龟车速
- 完善语音提醒体系与提前靠边场景
  - 新增控速事件语音提醒（匝道/电子眼/收费站/隧道/红灯），逐项开关且与变道提醒共用间隔、变道优先
  - 新增语音指令确认播报与头枕出声开关，语音设置统一迁移到语音 tab，变道提醒支持分项控制
  - 提前靠左/靠右时 SR 确认目标侧无车道则不再打灯变道，仅保留语音提醒并更新悬浮窗展示

## 其他
- 【应用管理】一键授权默认不开启后台运行/自启动并支持一键禁止
- 【乾坤视窗】新增应用任务跨屏模式
- 【乾坤视窗】新增自动隐藏应用栏开关
- 【乾坤守护】锁车信号读取失败时按已锁车兜底
- 【Traccar】新增车辆数据上报模块
- 【核心引擎】支持远程上电收到恢复功能运行
- 【锁车提醒】修复定位地址重复
- 【全局手势】「应用抽屉」支持切换类 macOS 启动台样式
- 【高德导航辅助】修复 ETA 到达时间文案重复与加粗异常
- 分享码兼容 4 至 6 位分享码导入
- 【应用管理】一键授权默认不开启后台运行/自启动并支持一键禁止
- 【限制后台运行】“软件级的应用冰箱”来了，增加应用运行限制功能，类似黑域/冰箱的自定管理后台应用的机制
