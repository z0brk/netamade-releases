# 🚀 测试版更新

- 【任务系统】修复「车门变化触发」等工作流条件在更新后不触发的问题
- 【锁车提醒】修复「未锁车提醒」在车辆长时间停放未启动时不触发的问题：人员检测信号完全静默满一段时间后按车内无人处理，不再一直卡在有人在场
- 【美式 Dock】修复「弹出面板」不跟随深色/浅色主题自动切换、始终显示为深色的问题：空调调节、冰箱调节、音量、驾驶与能源、灯光控制 5 种弹出面板现已跟随主题切换，深色主题下外观不变
- 【OP助手】「导航发送」持续向 OP 透传电子眼信息：只要高德播报电子眼就把类型、限速和距离发给 OP，不再受「电子眼减速」开关和减速距离窗口限制，OP 的 CAM 提示可提前显示；无限速值的电子眼也会发送类型信息
- 【乾坤守护】修复「检测模板 -> 预警响应 -> 触发策略」修改不生效的问题：在模板中调整触发策略、判定时间窗或分类提醒级别后会被静默丢弃，守护仍按修改前的策略执行
- 【乾坤守护】修复在车内已锁车状态下启动守护时误播「锁车音效」的问题：启动时读到"当前已锁车"不再当作一次锁车动作；真实锁车/解锁场景的音效行为不变
- 【OP助手】修复「转向与掉头」在掉头时不打方向灯的问题，左掉头和右掉头现在会触发对应方向灯
- 【温差自动除雾】新增场景，上车或行驶中检测到车内外温差过大（默认 10°C，可调）时弹窗询问，确认后开启前挡除雾，覆盖冬季低温和夏季地库出行两类起雾场景
- 【钥匙电量低提醒】新增场景，智能钥匙电量低于阈值（默认 25%，可调）时语音提醒并可远程推送，默认每 7 天最多提醒一次（支持 GT/S/猎装，哪吒 L 无钥匙电量信号）
- 【摄像头管理】提升 22S「四路拍照」成功率：远程拍照、P 档驻车和确认下车开门时，常规拍照失败会自动切换备用拍照方式；上车、锁车和下电阶段不会补拍
- 【低速安心】修复车速降到设定速度以下后不再自动开启的问题（D 档起步、转向灯触发不受影响）
- 【行车记录仪】修复录像画面上的转向灯标记不随实际转向灯变化的问题
- 【任务系统】「Openpilot 控制」任务新增「切换领航」选项：执行时读取当前领航状态，开启时关闭、关闭时开启；当前状态无法确定时不执行
- 【OP助手】「爆闪」执行前自动将大灯切换为自动模式（不恢复原状态）；页面新增醒目提示：只有在夜晚且大灯为自动状态时才能爆闪，闪光间隔最低 50ms
- 【盲区辅助】「近距离跟车」和「自动驻车」的画面退出改为按车辆状态判断，不再使用固定倒计时：障碍离开或驻车退出后经短暂状态确认关闭（期间状态恢复则保持显示），起步、超速或离开 D 档时立即关闭；同时修复转向灯等其它触发关闭时会把这两个画面一并倒计时关闭的问题，设置页不再展示这两个触发器的「自动关闭倒计时」
- 【盲区辅助】修复「近距离跟车」画面在雷达无回波时被过早关闭的问题：前向观察雷达只在极近距离给出瞬时距离，丢失回波不再当作障碍离开处理（确认时间延长到 5 秒），恢复有效回波且距离重新低于阈值时继续维持显示
- 【行程能耗】修复短暂驻车后推送空「行程能耗报告」的问题：短暂挂 D 档后立即回 P 档、没有里程和能耗变化的行程不再推送远程报告，行程记录仍正常保留
- 【盲区辅助】修复「近距离跟车」在 P 档驻车时也反复触发的问题：应用在 P 档启动后档位门限不再被默认值放行，只有真实挂入 D 档才会触发；换挡、超速时立即关闭的行为不变
- 【车机音效】新增「车内共存播放」，播放时不暂停媒体；优化「车内外同时播放」「仅车外播放」，并支持为每个音效单独设置车外增益（默认跟随全局；全局默认 12 dB，最高 90 dB）
- 【媒体控制】重做「一键喊话」：支持录音外放与独立增益，可添加、播放和删除 TTS 文本及声音库音频；打开后直接显示可折叠、可拖动的独立悬浮窗
- 【车机音效】修复「车内共存播放」用于挂挡音效后声音异常变大的问题
- 【氛围灯】修复「氛围灯 -> 编排 -> 心跳模式」开启心跳后亮度不再变化的问题：心跳改为按写入节拍分档输出，强拍、弱拍峰值不再被高频写入限频抹平，非心跳编排行为不变
- 【乾坤视界】修复展开预览后「一键喊话」「沉浸驾驶」悬浮窗被隐藏后不再显示的问题，收起乾坤视界后自动恢复
- 【远程车控】新增「一键喊话」，车辆在线时可发送文字并仅通过车外扬声器播报
- 【任务系统】优化「一键喊话」录音外放：降低持续环境噪声、限制高增益峰值并接入回声消除，减少杂音、爆音和外放回串；最终效果受车辆音频通路影响
- 【任务系统】「播放声音」支持在车外播放时自定义增益大小，未设置时跟随全局车外增益
- 【一键喊话】新增「自动开始喊话」开关，开启后每次打开悬浮窗会自动开始喊话，默认关闭
- 【一键喊话】修复悬浮窗显示时先以小尺寸出现再变大的问题，首帧即为正确大小
- 【无麦K歌】「无麦K歌」耳返链路与一键喊话保持一致，声音先经过降噪和回声消除再外放，降噪或回声消除初始化失败时自动回退原始声音
