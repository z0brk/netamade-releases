# 🚀 测试版更新

- 【乾坤视界】修复「展开尺寸」设置后自动恢复为旧尺寸的问题。
- 【应用启动器】修复放大字体后，座椅加热等快捷操作图标间距不一致的问题。
- 【应用启动器】新增「显示设置 -> 图标行距」，支持 0–48dp 即时调整，主副屏分别保存。
- 【控制中心】新增「操作后自动隐藏」，快捷功能执行成功后隐藏悬浮窗；使用 Dock 数字座舱组件时默认开启，其余情况默认关闭，支持保存手动选择。
- 【控制中心】优化「总览」设置布局，仅总开关保留大卡片，其余采用普通开关行，支持滚动查看下方设置。
- 【车辆电池诊断】区分「电池健康度」与「BMS 健康度」，分别使用 kWh 估算和 BMS 可用 Ah 与设计 Ah 的比值，详情、历史记录及变化比较同步展示。
- 【车辆电池诊断】云端数据获取失败时，弹窗提示前往车机「应用中心 -> 个人中心」登录账号后重新获取，已登录时可尝试退出后重新登录。
- 【应用管理】新增「一键冻结应用」，按主副屏默认勾选已安装的参考应用，支持取消勾选并批量冻结，展示处理进度与失败原因；醒目提示列表包含网易云音乐、QQ 音乐等软件，冻结可能影响原车氛围灯，请仅选择不使用的软件。
- 【应用管理】将主副屏切换、应用类型和状态筛选移至列表上方，减少左侧筛选项；冻结状态改为单选，修复快速切屏时旧查询覆盖当前列表的问题。
- 【限制后台运行】新增「导入参考列表」，支持按主副屏勾选符合条件的参考应用，统一设置为自动停止、自动冻结或不控制；已有配置同步更新限制方式，保留总开关和宽限时间，并显示跳过数量、失败原因及音乐软件和原车氛围灯风险提示。
- 【乾坤视界】修复「循环录制」落盘失败后重复报错的问题，保留首次失败提示。
- 【美式 Dock】新增「任务组件」动态颜色，可切换任务启用后高亮图标，复合组件及横向、纵向容器内的子任务同步支持。
- 【无线热点】修复「亮屏自动开启」在车机无线模块恢复较慢时容易失败的问题，避免重复开启干扰恢复，并在系统恢复失败后自动补偿重试一次。
- 【智驾小蓝灯】修复「蓝牙连接」静默失联后无法自动恢复的问题，连接超时后会清理失效连接并有限重试，避免长期显示断开。
- 【应用管理】「一键授权」支持为声明通知监听服务的应用开通通知使用权，并按主副用户分别确认系统授权结果。
- 【乾坤视界】修复「录制启动」失败后无法自动恢复的问题，失败清理完成后会重新检查条件并自动尝试启动。
- 【应用增强】优化解锁车辆后增强的应用卡死白屏问题、增加增强应用闪退时自动解除增强的机制
- 【EasyTier】新增「独立组网」页面，支持启停 EasyTier、编辑 machineId/hostname/instanceId 及网络配置，并展示连接状态和最近日志
- 【乾坤守护】修复「启动」等待录像收尾导致锁车后启动失败的问题，先完成锁车不断电，再继续处理录像收尾。
- 【OP 助手】优化「领航悬浮窗」明暗主题颜色，独立显示时跟随 Material 主题，提升深色和浅色模式下的可读性。
- 【OP 助手】修复主开关关闭后仍运行自动变道和 SR 后台链路并持续产生日志的问题。
- 【OP 助手】优化「自动变道」日志，仅保留变道准备、指令下发、确认、执行、完成、取消和超时等关键节点。
- 【乾坤守护】优化「震动检测」日志，默认关闭采样和未达阈值诊断，仅保留达到阈值并触发预警的结果。
- 【限制后台运行】修复自动冻结应用在应用列表、应用启动器和美式 Dock 中不显示的问题，冻结状态下仍可选择并解冻启动。
- 【美式 Dock】新增「本次行程 -> 隐藏单位」设置，开启后里程、电耗、时长和均速仅显示数值。
